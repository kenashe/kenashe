---
title: 'Hello'
description: 'A short hello to test the blog template, RSS feed, and JSON-LD pipeline.'
pubDate: 2026-05-01
tags: ['meta']
draft: true
---

## Hello, world

This is a placeholder post. It exists to exercise the prose styles, the RSS
feed, the structured data, and the [link styling](https://kenashe.ai). It will
be deleted before the site goes live.

A second paragraph, just to confirm vertical rhythm reads well across two
paragraphs of body copy in Newsreader. Italic words look like _this_, and bold
words look like **this**.

> A blockquote, set in italic, to make sure the indentation and color tokens
> resolve correctly in both light and dark modes.

Here is a small code block to verify the mono font and surface tones:

```ts
function greet(name: string): string {
  return `Hello, ${name}.`;
}

greet('world');
```

That's it.
